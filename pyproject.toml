[project]
name = "lingq-fa"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aeneas @ git+https://github.com/daxida/aeneas",
    "loguru>=0.7.3",
    "mutagen>=1.47.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3.4",
]

[project.scripts]
fa = "forced_alignment:main"
